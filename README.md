# Homebrew tap for OpenCues

```bash
brew install opencues/opencues/opencues
```

AI anywhere you type — https://opencues.com · main repo: https://github.com/opencues/opencues

The formula tracks the npm release (`opencues` on npmjs); it is bumped as part
of each product release.
